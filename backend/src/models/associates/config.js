// This file will contain functions for defining model associations.
// Each function should be named `${modelName}Model` and accept a `db` object as an argument.
// Example:
// exports.UserModel = (db) => { db.User.hasMany(db.Post, { foreignKey: 'userId' }); };
// exports.PostModel = (db) => { db.Post.belongsTo(db.User, { foreignKey: 'userId' }); };
